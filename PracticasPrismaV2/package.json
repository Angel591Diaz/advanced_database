{
  "name": "bibliotecaprisma",
  "version": "1.0.0",
  "description": "API y gestión de base de datos para una biblioteca digital con Prisma.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:migrate": "npx prisma migrate dev",
    "db:seed": "npx prisma db seed",
    "db:reset": "npx prisma migrate reset"
  },
  "keywords": [
    "prisma",
    "express",
    "mysql",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^5.1.0"
  },
  "dependencies": {
    "@prisma/client": "^5.1.0",
    "express": "^4.21.2"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
